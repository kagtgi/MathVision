node_modules/
# `build/` KHÔNG được ignore: đó là buildResources của electron-builder và chứa
# icon.ico mà package.json trỏ vào. Ignore nó thì runner clone sạch sẽ build ra
# exe không có icon. (Vite xuất ra `dist/`, không liên quan tới `build/`.)
dist/
coverage/
release/
release-new/
demo/
.DS_Store
*.log
.env*
!.env.example

# Ảnh cắt tạm của scripts/probe-figure-pipeline.mjs. Vite copy cả public/ vào dist/ nên một
# lượt chạy hỏng (hoặc --keep) sẽ nhét hình đề thi vào bản đóng gói.
public/probe-figs/
